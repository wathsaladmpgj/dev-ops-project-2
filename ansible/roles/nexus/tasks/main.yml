---
- name: Install docker
  apt:
    name: docker.io
    state: present
    update_cache: yes

- name: Enable docker
  systemd:
    name: docker
    enabled: yes
    state: started

# Required so community.docker works properly
- name: Install Python Docker SDK (required by Ansible docker modules)
  apt:
    name: python3-docker
    state: present
    update_cache: yes

- name: Create Nexus data dir
  file:
    path: /opt/nexus-data
    state: directory
    mode: "0777"

- name: Run Nexus container
  community.docker.docker_container:
    name: nexus
    image: "sonatype/nexus3:{{ nexus_version | default('3.70.1') }}"
    state: started
    restart_policy: always
    ports:
      - "8081:8081"
    volumes:
      - "/opt/nexus-data:/nexus-data"
